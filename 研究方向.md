# Class Imbalance (3)

• Continual learning with class imbalance (non-stationary datasets), e.g.
bankruptcy prediction (1999~2018) (Kaggle), stock prediction (學長的論文), and
time series medical datasets (UCI)
• 資料集切割: 例如 a. 1999~2011 as the historical data, 2012~2014 as the new
operating data, and 2015~2018 for the testing data; b. 5-fold cross validation:
1
st+2nd folds as the historical data, 3rd + 4th folds as the new operating data, and
the 5th fold for the testing data
• Baselines: 1. Re-training the model using historical and new operating data; 2.
fine-tuning the model solely using new data
• Ensemble classifiers with/without dynamic classifier selection/dynamic ensemble
selection:

1. ‘Old’ models 1/2/3 (trained by historical data with under-/over-/hybrid
sampling)
2. ‘New’ models 4/5/6 (trained by new operating data with under-/over-/hybrid
sampling)
3. combination methods: a. two combined models: pairs of ‘Old’ and ‘New’
models; b. three combined models: two ‘Old’ + one ‘New’ or one ‘Old’ + two ‘New’
models; c. four combined models; d. five combined models; e. six combined models
• Study II: the effect of feature selection on the ensemble classifiers
